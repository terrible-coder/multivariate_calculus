import { Matrix } from "./matrix";

/*
I don't know how to work with tensors yet.
The class will therefore be empty till I learn about them properly.
*/
export class Tensor extends Matrix {
	constructor(matrix2x2:number[][]) {
		super(matrix2x2);
	}
}